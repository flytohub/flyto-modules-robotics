# Tasks

- [ ] Expose the robot's capability catalog on the HTTP gateway and read bounds
      from it, replacing the constants in `plan.py` (ROADMAP 1)
- [ ] Add the generic `robotics.command` step (ROADMAP 2)
- [ ] Install alongside a real `flyto-core` and confirm the steps reach the canvas
- [ ] Decide whether to publish to PyPI, and under which account
